import React from 'react'
import { useSelector } from 'react-redux';

const CourseAccessDetail = ({ id }) => {
    const { user } = useSelector((state) => state.auth);
  return (
    <div>
      cfdcfds
    </div>
  )
}

export default CourseAccessDetail
